<config>
	<!-- Dependent parameters -->
	<param name="externalizable.help.url">/${app.context.name}/static/help/</param>
	<param name="externalizable.images.url">/${app.context.name}/kr/static/images/</param>
	<param name="kr.externalizable.images.url">/${app.context.name}/kr/static/images/</param>
	<param name="kra.externalizable.images.url">/${app.context.name}/static/images/</param>

	<param name="application.url">${app.host}/${app.context.name}</param>
	<param name="kr.url">${application.url}/kr</param>
	<param name="appserver.url">http://127.0.0.1:8080</param>
	<param name="ken.url">${application.url}/ken</param>
	<param name="kew.url">${application.url}/kew</param>
	<param name="kim.url">${application.url}/kim</param>
	<param name="ksb.url">${application.url}/ksb</param>
	<param name="rice.url">${application.url}/kr</param>
	<param name="workflow.url">${application.url}/kew</param>
	<param name="serviceServletUrl">${application.url}/remoting/</param>
	
	<param name="workflow.documentsearch.base.url">kew/DocumentSearch.do?backLocation=${application.url}</param>

	<param name="attachments.pending.directory">${java.io.tmpdir}</param>
	<param name="attachments.directory">${java.io.tmpdir}</param>
	<param name="kuali.docHandler.url.prefix">${app.host}/${app.context.name}</param>

	<!-- Ken stuffs... -->
	<!-- stock notification system configuration properties -->
	<param name="notification.basewebappurl">${app.host}/${app.context.name}/ken</param>

	<!-- image overrides -->
	<param name="application.custom.image.url.copyoneper">/${app.context.name}/static/images/buttonsmall_copyallperiods.gif</param>
	<param name="application.custom.image.url.copyallperiods">/${app.context.name}/static/images/buttonsmall_copyoneper.gif</param>
</config>