CREATE SEQUENCE SEQ_PROP_UNIT_ADMIN_ID INCREMENT BY 1 START WITH 1;

ALTER TABLE PROP_UNIT_ADMINISTRATORS
MODIFY (UNIT_ADMINISTRATOR_TYPE_CODE    varchar2(3));

ALTER TABLE PROP_UNIT_ADMINISTRATORS
    ADD ( CONSTRAINT FK_UNIT_ADMIN_TYPE_CODE
	FOREIGN KEY(UNIT_ADMINISTRATOR_TYPE_CODE)
	REFERENCES UNIT_ADMINISTRATOR_TYPE(UNIT_ADMINISTRATOR_TYPE_CODE));

ALTER TABLE PROP_UNIT_ADMINISTRATORS
    ADD ( CONSTRAINT FK_ADMINISTRATOR
	FOREIGN KEY(ADMINISTRATOR)
	REFERENCES PERSON(PERSON_ID));

commit;