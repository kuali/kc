-- Table Script 
CREATE TABLE GROUP_TYPES ( 
    GROUP_TYPE_CODE NUMBER(3,0) NOT NULL, 
    GROUP_NAME VARCHAR2(200) NOT NULL, 
    UPDATE_TIMESTAMP DATE NOT NULL, 
    UPDATE_USER VARCHAR2(60) NOT NULL, 
    VER_NBR NUMBER(8,0) DEFAULT 1 NOT NULL, 
    OBJ_ID VARCHAR2(36) DEFAULT SYS_GUID() NOT NULL);


-- Primary Key Constraint 
ALTER TABLE GROUP_TYPES 
ADD CONSTRAINT PK_GROUP_TYPES 
PRIMARY KEY (GROUP_TYPE_CODE);



-- View for Coeus compatibility 
CREATE OR REPLACE VIEW OSP$GROUP_TYPES AS SELECT 
    GROUP_TYPE_CODE, 
    GROUP_NAME, 
    UPDATE_TIMESTAMP, 
    UPDATE_USER
FROM GROUP_TYPES;


INSERT INTO GROUP_TYPES ( GROUP_TYPE_CODE, GROUP_NAME, UPDATE_TIMESTAMP, UPDATE_USER ) 
VALUES ( 1, 'IRB', sysdate, user ); 
INSERT INTO GROUP_TYPES ( GROUP_TYPE_CODE, GROUP_NAME, UPDATE_TIMESTAMP, UPDATE_USER ) 
VALUES ( 2, 'Proposal', sysdate, user ); 
INSERT INTO GROUP_TYPES ( GROUP_TYPE_CODE, GROUP_NAME, UPDATE_TIMESTAMP, UPDATE_USER ) 
VALUES ( 3, 'Others', sysdate, user ); 


