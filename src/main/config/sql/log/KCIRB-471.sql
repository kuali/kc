
ALTER TABLE PROTOCOL_ACTIONS
ADD (ACTUAL_ACTION_DATE DATE DEFAULT sysdate NOT NULL);

CREATE OR REPLACE VIEW OSP$PROTOCOL_ACTIONS AS SELECT
  PROTOCOL_NUMBER,
  SEQUENCE_NUMBER,
  ACTION_ID,
  PROTOCOL_ACTION_TYPE_CODE,
  SUBMISSION_NUMBER,
  COMMENTS,
  ACTUAL_ACTION_DATE as UPDATE_TIMESTAMP,
  UPDATE_USER,
  ACTION_DATE
FROM PROTOCOL_ACTIONS; 

commit;

