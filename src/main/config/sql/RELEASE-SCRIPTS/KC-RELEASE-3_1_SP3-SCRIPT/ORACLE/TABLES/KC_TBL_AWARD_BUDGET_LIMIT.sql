CREATE TABLE AWARD_BUDGET_LIMIT (
    BUDGET_LIMIT_ID NUMBER(22,0) NOT NULL,
    AWARD_ID NUMBER(22,0),
    BUDGET_ID NUMBER(22,0),
    LIMIT_TYPE VARCHAR2(100) NOT NULL,
    LIMIT_AMOUNT NUMBER(12,2),
    VER_NBR NUMBER(8,0) NOT NULL,
    UPDATE_TIMESTAMP DATE NOT NULL,
    UPDATE_USER VARCHAR2(60) NOT NULL,
    OBJ_ID VARCHAR2(36) NOT NULL
);

ALTER TABLE AWARD_BUDGET_LIMIT ADD CONSTRAINT PK_AWARD_BUDGET_LIMIT
  PRIMARY KEY (BUDGET_LIMIT_ID);