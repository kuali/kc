CREATE OR REPLACE VIEW OSP$AWARD_STATUS(STATUS_CODE,DESCRIPTION,UPDATE_TIMESTAMP,UPDATE_USER) AS 
SELECT STATUS_CODE,
       DESCRIPTION,
       UPDATE_TIMESTAMP,
       UPDATE_USER
FROM   AWARD_STATUS;