ALTER TABLE BUDGET ADD BUDGET_ID NUMBER (12, 0) ;

ALTER TABLE BUDGET DROP PRIMARY KEY CASCADE;

ALTER TABLE BUDGET MODIFY (PROPOSAL_NUMBER NULL,
	BUDGET MODIFY VERSION_NUMBER NULL);

ALTER TABLE BUDGET 
    ADD CONSTRAINT PK_BUDGET 
            PRIMARY KEY (BUDGET_ID) ;