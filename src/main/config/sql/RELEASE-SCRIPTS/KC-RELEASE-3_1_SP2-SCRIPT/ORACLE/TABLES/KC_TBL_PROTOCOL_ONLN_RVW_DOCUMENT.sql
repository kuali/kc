-- The following command must be run together with the commands in TBL_PROTOCOL_ONLN_RVWS --
-- Adding it here for convenience --
-- ALTER TABLE PROTOCOL_ONLN_RVW_DOCUMENT MODIFY (DOCUMENT_NUMBER VARCHAR2(40));