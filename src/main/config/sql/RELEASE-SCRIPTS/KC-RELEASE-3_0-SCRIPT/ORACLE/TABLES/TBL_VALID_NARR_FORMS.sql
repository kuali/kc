CREATE TABLE VALID_NARR_FORMS (
  	VALID_NARR_FORMS_ID NUMBER(12,0) NOT NULL,
  	FORM_NAME VARCHAR2(100) NOT NULL ENABLE, 
	NARRATIVE_TYPE_CODE VARCHAR2(3) NOT NULL ENABLE, 
	MANDATORY VARCHAR2(3), 
	UPDATE_TIMESTAMP DATE NOT NULL ENABLE, 
	UPDATE_USER VARCHAR2(60) NOT NULL ENABLE
);
ALTER TABLE VALID_NARR_FORMS 
	ADD CONSTRAINT PK_VALID_NARR_FORMS 
	PRIMARY KEY (VALID_NARR_FORMS_ID);
	
