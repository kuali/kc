CREATE TABLE PROTOCOL_VOTE_RECUSED
(
  PROTOCOL_VOTE_RECUSED_ID DECIMAL(12, 0) NOT NULL,
  PROTOCOL_ID_FK DECIMAL(12) NOT NULL,
  SUBMISSION_ID_FK DECIMAL(12) NOT NULL,
  PERSON_ID VARCHAR(40) ,
  ROLODEX_ID VARCHAR(40) ,
  NON_EMPLOYEE_FLAG VARCHAR(1) NOT NULL,
  COMMENTS VARCHAR(2000),
  UPDATE_TIMESTAMP DATE NOT NULL,
  UPDATE_USER VARCHAR(60) NOT NULL,
  VER_NBR DECIMAL(8),
  OBJ_ID VARCHAR(36)
);

ALTER TABLE PROTOCOL_VOTE_RECUSED 
ADD PRIMARY KEY PROTOCOL_VOTE_RECUSED_PK (PROTOCOL_VOTE_RECUSED_ID);
