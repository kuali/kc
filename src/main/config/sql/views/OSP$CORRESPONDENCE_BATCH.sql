-- View for Coeus compatibility 
CREATE OR REPLACE VIEW OSP$CORRESPONDENCE_BATCH AS SELECT 
    BATCH_CORRESPONDENCE_TYPE_CODE AS CORRESP_BATCH_TYPE_CODE, 
    DESCRIPTION, 
    DEFAULT_TIME_WINDOW, 
    FINAL_ACTION_TYPE_CODE, 
    FINAL_ACTION_CORRESP_TYPE, 
    UPDATE_TIMESTAMP, 
    UPDATE_USER
FROM BATCH_CORRESPONDENCE;
