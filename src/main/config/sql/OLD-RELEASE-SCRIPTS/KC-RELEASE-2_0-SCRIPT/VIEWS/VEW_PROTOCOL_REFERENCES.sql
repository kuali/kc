CREATE OR REPLACE VIEW osp$protocol_references AS 
SELECT PROTOCOL_NUMBER,
       SEQUENCE_NUMBER,
       PROTOCOL_REFERENCE_NUMBER,
       PROTOCOL_REFERENCE_TYPE_CODE,
       REFERENCE_KEY,
       APPLICATION_DATE,
       APPROVAL_DATE,
       COMMENTS,
       UPDATE_TIMESTAMP,
       UPDATE_USER
FROM   PROTOCOL_REFERENCES;