CREATE OR REPLACE VIEW OSP$TEMPLATE_CONTACT(TEMPLATE_CODE,CONTACT_TYPE_CODE,ROLODEX_ID,UPDATE_TIMESTAMP,UPDATE_USER) AS 
SELECT AWARD_TEMPLATE_CODE,
       CONTACT_TYPE_CODE,
       ROLODEX_ID,
       UPDATE_TIMESTAMP,
       UPDATE_USER
FROM   AWARD_TEMPLATE_CONTACT;