-- Table Script 
CREATE TABLE AWARD_NOTEPAD ( 
    AWARD_NOTEPAD_ID NUMBER(12,0) NOT NULL, 
    AWARD_NUMBER VARCHAR2(8) NOT NULL, 
    ENTRY_NUMBER NUMBER(4,0) NOT NULL, 
    COMMENTS CLOB NOT NULL, 
    RESTRICTED_VIEW CHAR(1) NOT NULL, 
    UPDATE_TIMESTAMP DATE NOT NULL, 
    UPDATE_USER VARCHAR2(60) NOT NULL, 
    VER_NBR NUMBER(8,0) DEFAULT 1 NOT NULL, 
    OBJ_ID VARCHAR2(36) DEFAULT SYS_GUID() NOT NULL);


-- Primary Key Constraint 
ALTER TABLE AWARD_NOTEPAD 
ADD CONSTRAINT PK_AWARD_NOTEPAD 
PRIMARY KEY (AWARD_NOTEPAD_ID);

-- *************** MODIFIED PRIMARY KEY COLUMN - Introduced new primary key for existing composite key ************ 
-- ALTER TABLE AWARD_NOTEPAD 
-- ADD CONSTRAINT PK_AWARD_NOTEPAD 
-- PRIMARY KEY (AWARD_NUMBER, ENTRY_NUMBER);
-- *************** MODIFIED PRIMARY KEY COLUMN - Introduced new primary key for existing composite key ************ 
 

-- *************** UNIQUE CONSTRAINT DEFINED FOR COMPOSITE KEY COLUMNS ************ 
ALTER TABLE AWARD_NOTEPAD 
ADD CONSTRAINT UQ_AWARD_NOTEPAD 
UNIQUE (AWARD_NUMBER, ENTRY_NUMBER);
