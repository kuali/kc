CREATE TABLE RATE_CLASS_TYPE
(
  RATE_CLASS_TYPE   CHAR(1) CONSTRAINT RATE_CLASS_TYPEN1 NOT NULL,
  DESCRIPTION       VARCHAR2(200) CONSTRAINT RATE_CLASS_TYPEN2 NOT NULL,
  UPDATE_TIMESTAMP  DATE CONSTRAINT RATE_CLASS_TYPEN3 NOT NULL,
  UPDATE_USER       VARCHAR2(8) CONSTRAINT RATE_CLASS_TYPEN4 NOT NULL,
  VER_NBR           NUMBER(8)                   DEFAULT 1 CONSTRAINT RATE_CLASS_TYPEN5 NOT NULL,
  OBJ_ID            VARCHAR2(36)           DEFAULT SYS_GUID() CONSTRAINT RATE_CLASS_TYPEN6 NOT NULL,
CONSTRAINT "PK_RATE_CLASS_TYPE_KRA" PRIMARY KEY ("RATE_CLASS_TYPE") ENABLE
)
/
