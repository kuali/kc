CREATE TABLE COST_SHARE_TYPE ( 
	COST_SHARE_TYPE_CODE	NUMBER(3,0) constraint COST_SHARE_TYPE_CODEN0 NOT NULL ENABLE,
	DESCRIPTION				VARCHAR2(200) constraint COST_SHARE_TYPE_CODEN1 NOT NULL ENABLE,
    UPDATE_TIMESTAMP       	DATE constraint COST_SHARE_TYPE_CODEN2 NOT NULL ENABLE,
    UPDATE_USER            	VARCHAR2(60) constraint COST_SHARE_TYPE_CODEN3 NOT NULL ENABLE,
    VER_NBR 				NUMBER(8,0) DEFAULT 1  constraint COST_SHARE_TYPE_CODEN4 NOT NULL ENABLE,
	OBJ_ID 					VARCHAR2(36) DEFAULT SYS_GUID()  constraint COST_SHARE_TYPE_CODEN5 NOT NULL ENABLE,
    CONSTRAINT PK_COST_SHARE_TYPE PRIMARY KEY(COST_SHARE_TYPE_CODE) ENABLE
)