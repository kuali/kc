CREATE TABLE NARRATIVE_ATTACHMENT 
   (	PROPOSAL_NUMBER NUMBER(12,0) constraint NARRATIVE_ATTACHMENTN1 NOT NULL ENABLE, 
	MODULE_NUMBER NUMBER(4,0) constraint NARRATIVE_ATTACHMENTN2 NOT NULL ENABLE, 
	NARRATIVE_DATA BLOB, 
	FILE_NAME VARCHAR2(150),
	CONTENT_TYPE VARCHAR2(50),
	UPDATE_USER VARCHAR2(8) constraint NARRATIVE_ATTACHMENTN3 NOT NULL ENABLE, 
	UPDATE_TIMESTAMP DATE constraint NARRATIVE_ATTACHMENTN4 NOT NULL ENABLE, 
	 VER_NBR NUMBER(8,0) DEFAULT 1  constraint  NARRATIVE_ATTACHMENTN5  NOT NULL ENABLE,
	OBJ_ID VARCHAR2(36) DEFAULT SYS_GUID()  constraint  NARRATIVE_ATTACHMENTN6  NOT NULL ENABLE,
	CONSTRAINT PK_NARRATIVE_ATTACHMENT_KRA PRIMARY KEY (PROPOSAL_NUMBER, MODULE_NUMBER) ENABLE
)
/
ALTER TABLE NARRATIVE_ATTACHMENT ADD (CONSTRAINT FK_NARRATIVE_ATTACHMENT_KRA FOREIGN KEY (PROPOSAL_NUMBER, MODULE_NUMBER)
	  REFERENCES NARRATIVE (PROPOSAL_NUMBER, MODULE_NUMBER) ON DELETE CASCADE) 
