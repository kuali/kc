CREATE TABLE AWARD_PMC (
  AWARD_PMC_ID          NUMBER(12,0) NOT NULL,
  PERSON_MASS_CHANGE_ID NUMBER(12,0) NOT NULL,
  INVESTIGATOR          CHAR(1) DEFAULT 'N' NOT NULL,
  CONTACT_PERSON        CHAR(1) DEFAULT 'N' NOT NULL,
  FOREIGN_TRIP          CHAR(1) DEFAULT 'N' NOT NULL,
  UNIT_ADMINISTRATOR    CHAR(1) DEFAULT 'N' NOT NULL,
  UPDATE_USER           VARCHAR2(60) NOT NULL, 
  UPDATE_TIMESTAMP      DATE NOT NULL, 
  OBJ_ID                VARCHAR2(36) NOT NULL,
  VER_NBR               NUMBER(8,0) DEFAULT 1 NOT NULL
)
/
ALTER TABLE AWARD_PMC ADD CONSTRAINT PK_AWARD_PMC
  PRIMARY KEY (AWARD_PMC_ID)
/
