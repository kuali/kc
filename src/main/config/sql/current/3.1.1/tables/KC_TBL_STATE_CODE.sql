DECLARE temp NUMBER;
BEGIN
    SELECT COUNT(*) INTO temp FROM USER_TABLES WHERE TABLE_NAME = 'STATE_CODE';
    IF temp > 0 THEN 
        EXECUTE IMMEDIATE 'DROP TABLE STATE_CODE';
    END IF;
END;
/
