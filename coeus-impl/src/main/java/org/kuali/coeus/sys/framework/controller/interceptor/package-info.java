/**
 * This package contains a system-level code related to the
 * interceptor pattern.  These could be spring mvc interceptors or
 * struts servlet filters.  Basically, anything that wraps and
 * adds behavior to a controller.
 */
package org.kuali.coeus.sys.framework.controller.interceptor;