CREATE TABLE PROTOCOL_NOTEPAD
   (PROTOCOL_NOTEPAD_ID NUMBER(12,0) NOT NULL ENABLE,
    PROTOCOL_ID_FK NUMBER(12,0) NOT NULL, 
    PROTOCOL_NUMBER VARCHAR2(20) NOT NULL, 
    SEQUENCE_NUMBER NUMBER(4,0) NOT NULL ENABLE,
    ENTRY_NUMBER NUMBER(4,0) NOT NULL,
    COMMENTS CLOB,
    RESTRICTED_VIEW CHAR(1) NOT NULL,
    NOTE_TYPE VARCHAR2(60) NOT NULL,
    VER_NBR NUMBER(8,0) DEFAULT 1 NOT NULL ENABLE,
    OBJ_ID VARCHAR2(36) DEFAULT SYS_GUID()  NOT NULL ENABLE,
    UPDATE_TIMESTAMP DATE NOT NULL ENABLE,
    UPDATE_USER VARCHAR2(10) NOT NULL ENABLE);
	
ALTER TABLE PROTOCOL_NOTEPAD
    ADD CONSTRAINT PK_PROTOCOL_NOTEPAD
    PRIMARY KEY (PROTOCOL_NOTEPAD_ID);
	
ALTER TABLE PROTOCOL_NOTEPAD 
    ADD CONSTRAINT FK_P_NOTEPAD_PROTOCOL 
    FOREIGN KEY (PROTOCOL_ID_FK) 
    REFERENCES PROTOCOL (PROTOCOL_ID);
	
ALTER TABLE PROTOCOL_NOTEPAD 
    ADD CONSTRAINT UQ_PROTOCOL_NOTEPAD 
    UNIQUE (PROTOCOL_NUMBER, ENTRY_NUMBER);
	
