DECLARE
    C NUMBER;
    BEGIN
        SELECT COUNT(*) INTO C FROM USER_SEQUENCES WHERE SEQUENCE_NAME = 'SEQ_BUDGET_PER_SAL_DET_ID';
        IF C = 0 THEN
            EXECUTE IMMEDIATE 'CREATE SEQUENCE SEQ_BUDGET_PER_SAL_DET_ID INCREMENT BY 1 START WITH 1 NOCACHE';
        END IF;
    END;
/
