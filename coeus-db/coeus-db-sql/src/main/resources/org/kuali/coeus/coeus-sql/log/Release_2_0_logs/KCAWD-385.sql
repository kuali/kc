ALTER TABLE AWARD_APPROVED_SUBAWARDS
ADD (ORGANIZATION_ID  VARCHAR2(8));

ALTER TABLE AWARD_APPROVED_SUBAWARDS
    ADD ( CONSTRAINT FK_ORGANIZATION_ID
	FOREIGN KEY(ORGANIZATION_ID)
	REFERENCES ORGANIZATION(ORGANIZATION_ID));

commit;