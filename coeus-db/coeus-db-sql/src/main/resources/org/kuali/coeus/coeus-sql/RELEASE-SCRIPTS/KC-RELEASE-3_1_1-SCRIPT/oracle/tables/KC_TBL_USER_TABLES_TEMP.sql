DECLARE temp NUMBER;
BEGIN
    SELECT COUNT(*) INTO temp FROM USER_TABLES WHERE TABLE_NAME = 'USER_TABLES_TEMP';
    IF temp > 0 THEN 
        EXECUTE IMMEDIATE 'DROP TABLE USER_TABLES_TEMP';
    END IF;
END;
/
